[build]
  command = "npm install && npm run build"
  publish = "dist"
  base = "frontend"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.production.environment]
  VITE_API_URL = "YOUR_BACKEND_API_URL"
